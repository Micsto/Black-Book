<UserControl x:Class="TordsApplikation.View.ucPersonList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:model="clr-namespace:TordsApplikation.Model"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance d:Type={x:Type model:Person}}">
    <Grid>
        <!-- A simple usercontroll with 3 labels that i bind to the Personlist -->
        <StackPanel Orientation="Horizontal">
                <Label Content="{Binding Name}"></Label>
                <Label Content="{Binding Adress}"></Label>
                <Label Content="{Binding Phone}"></Label>
        </StackPanel>
    </Grid>
</UserControl>
